<!-- php artisan tinker --execute 'use App\Models\User; $user = User::withoutGlobalScopes()->where("id", 3080)->first(); if ($user) { $user->assignRole("super_admin"); echo "Gán super_admin cho user {$user->email} (ID {$user->id}) thành công".PHP_EOL; } else { echo "Không tìm thấy user ID 3080".PHP_EOL; }' -->
