---
description: Create a new Livewire v4 component
agent: build
model: alibaba-cn/qwen3.6-plus
---
Create a new Livewire v4 single-file component: $ARGUMENTS

Run: `php artisan make:livewire $ARGUMENTS`

After creation:
1. Review the generated component structure
2. Ensure it follows Livewire v4 SFC conventions
3. Add proper #[Title] attribute if it's a full-page component
4. Add #[Validate] attributes for properties
5. Implement mount() method if needed
6. Add proper Alpine.js integration for client-side interactivity

Component name: $ARGUMENTS
